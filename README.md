# Student Performance Predictor

Predicts if a student will pass or fail based on study hours, attendance, and class participation.

## How to Run
```bash
pip install -r requirements.txt
streamlit run app.py